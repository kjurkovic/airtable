apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "airtable.fullname" . }}-test-connection"
  labels:
    {{- include "airtable.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "airtable.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
